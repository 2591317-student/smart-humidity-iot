# TIẾN ĐỘ — Đợt tinh chỉnh theo góp ý nhóm (2026-07-02)

> Tóm tắt **đã làm gì / cần làm thêm / vướng mắc** cho đợt chỉnh sửa provisioning + Firebase + web
> theo phản hồi của nhóm. Đọc kèm [INDEX.md](INDEX.md) (bản đồ tổng) và [CONTRACT.md](CONTRACT.md)
> (nguồn sự thật interface).
>
> **Trạng thái deploy (cập nhật):**
> - ✅ **Web dashboard** (`smart-humidity-iot.web.app`) — đã deploy bản mới (đã verify live: có
>   `isDeviceOnline`/`lastSeen`).
> - ✅ **PWA cấu hình** (`...-provision.web.app`) — đã deploy bản mới (đã verify live: checkbox WiFi/MAC
>   + nút `/info`).
> - ❔ **Security Rules** — kiểm tra đã `firebase deploy --only database` chưa (không soi được từ ngoài).
>   ⚠️ Có thêm rules mới (`onlineTimeoutSec`, `pumpControlEnabled`, `pumpManualOn`) — cần deploy lại.
> - ⏳ **Firmware** — CHƯA nạp (cần board + USB). ⏳ **Push GitHub** — kiểm tra lại.
> - ⏳ **`web/js/app.js`, `web/js/db.js`, `web/index.html`** vừa sửa thêm (ngưỡng online cấu hình
>   được, fix bug alarm che UI, khối điều khiển bơm ẩn) — **CHƯA deploy lại**, cần
>   `firebase deploy --only hosting,database`.

---

## 1. Đã làm

### 1.0. Web dashboard — đợt sửa theo checklist (mới nhất)
- **Đưa ngưỡng online vào Firebase**: `/config/onlineTimeoutSec` (5–300s, admin chỉnh trên web) thay
  cho hardcode 15s — để align khi đổi chu kỳ ESP đẩy dữ liệu, khỏi sửa code.
- **`tools/presence-watcher`** (mới): script Node độc lập tự cập nhật CHÍNH field
  `/status/esp1Online` kể cả khi không ai mở web (đã xác nhận thư viện Firebase mobizt trên ESP32
  KHÔNG hỗ trợ `onDisconnect()` — kiểm tra trực tiếp source GitHub, 0 kết quả). Cần 1 máy chạy nó
  liên tục — không phải Cloud Functions, không tốn phí. ⚠️ Đây KHÔNG phải "chạy nền không cần máy
  nào" — vẫn cần ai đó bật nó lên.
- **Fix bug: cảnh báo cạn nước che mất nút đăng xuất/avatar.** Nguyên nhân: `#alarmBox` là
  `fixed z-50`, `header` là `sticky z-30` — cùng dính ở top, alarm đè lên header. Sửa: gộp
  alarm + header chung 1 khối `sticky` duy nhất (alarm nằm trong flow, đẩy header xuống thay vì đè
  lên). Đã verify bằng bounding-box test: không còn chồng nhau.
- **Khối "Điều khiển bơm thủ công"** (ẩn mặc định): `/config/pumpControlEnabled` — cờ ẩn, admin bật
  thẳng trong Firebase Console (không cần deploy lại web) để hiện nút bật/tắt bơm
  (`/config/pumpManualOn`). Chuẩn bị sẵn UI phía web trong lúc chờ Embed hoàn thành mục giám
  sát/điều khiển bơm (firmware CHƯA đọc field này — chờ Embed nối logic relay khi xong phần cứng).
- **Dropdown "Chế độ phun sương"** trong panel Deadband: `/config/mode` — 3 giá trị `off` /
  `continuous` / `intermittent` (mặc định `continuous`, khớp hành vi hiện tại). Web đã có UI +
  validate (rules + client) đầy đủ. ⚠️ **Firmware CHƯA đọc field này** — ESP1 hiện luôn chạy
  Deadband bất kể `mode` là gì; chờ Embed nối logic (bỏ qua Deadband khi `off`, tự định nghĩa
  `intermittent`) rồi mở rộng `pollConfig()`/`runDeadband()` để đọc thêm field này.
- **Fix an toàn:** `writeConfig()` đổi từ `set()` sang `update()` — `set()` cũ sẽ XOÁ SẠCH các field
  không nằm trong payload (vd sẽ xoá mất `pumpControlEnabled`/`pumpManualOn` mỗi khi admin lưu
  hset/deadband). `update()` chỉ ghi đè field được truyền, giữ nguyên phần còn lại.

### 1.1. Firmware (`firmware/`)
- **NVS-first, hardcode-fallback**: ESP đọc cấu hình từ Preferences/NVS trước; field nào trống thì dùng
  hằng số `HC_*` ở đầu `esp1_main/main.cpp` & `esp2_sensor/main.cpp`. → Nạp là chạy ngay khi dev, app vẫn
  ghi đè được khi cấu hình.
- **Vào provisioning bằng GIỮ nút BOOT (GPIO0)** lúc khởi động (thay cho "chưa cấu hình → SoftAP"). Không
  giữ nút thì chạy bình thường. Giống flow thiết bị smart home.
- **Tài khoản Firebase của thiết bị hardcode trong firmware** (`HC_DEV_EMAIL` = `device@smarthumidity.iot`,
  `HC_DEV_PASS` = `123456`) — app không hỏi 2 trường này nữa.
- **`POST /provision` partial update** (`lib/common/provisioning.cpp`): chỉ ghi field CÓ trong JSON (khớp
  cơ chế tick chọn của app), field vắng mặt giữ nguyên. Nhận cả `peerMac` lẫn `mac_gateway`.
- **Gộp ghi Firebase** (`esp1_main/main.cpp` `pushToFirebase`): `/sensor` + `/status` mỗi node ghi 1
  `setJSON` → **2 request/chu kỳ thay vì 9** → nhẹ tải TLS, đỡ nóng ESP.
- `esp1Online` ghi dạng **bool `true`** (khớp `.validate` trong rules).

### 1.2. App PWA (`mobile/`)
- Cắt gọn form còn **WiFi SSID + password + MAC** (bỏ role/deviceEmail/devicePassword/hset/deadband).
- **2 checkbox: WiFi / MAC** — tick mới gửi → cập nhật riêng từng phần (partial update).
- **QR chỉ để điền nhanh ô MAC** (nhập tay vẫn dùng), không còn là bước bắt buộc.
- **Nút "Kiểm tra kết nối" (GET `/info`)** + trạng thái online/offline của trình duyệt → chẩn đoán.

### 1.3. Web dashboard (`web/`)
- **Phát hiện online/offline theo `lastSeen`** thay vì cờ `esp1Online`: `online = (now − lastSeen) < 15s`,
  kèm **timer client 5s** tự lật "Mất kết nối" khi ESP ngừng đẩy. Không cần backend.
- **Ngưỡng 15s giờ nằm trong `/config/onlineTimeoutSec`** (admin chỉnh được trên web, 5–300s) — để
  align với chu kỳ ESP đẩy dữ liệu mà không cần sửa code/deploy lại khi đổi chu kỳ push.
- **`tools/presence-watcher`** (mới): script Node chạy độc lập, tự cập nhật CHÍNH field
  `/status/esp1Online` trong database kể cả khi không ai mở web (đã xác nhận thư viện Firebase
  mobizt trên ESP32 KHÔNG hỗ trợ `onDisconnect()` nên ESP không tự làm được). Cần một máy chạy nó
  liên tục — không phải Cloud Functions, không tốn phí.

### 1.4. Tài liệu
- Cập nhật: INDEX, README, CONTRACT, ARCHITECTURE, mobile/README, DEMO, HANDOFF-HARDWARE, NGHIEM-THU,
  SETUP, FIREBASE-GUIDE, mock-esp-server/README. Tất cả đã khớp thiết kế mới.

### 1.5. Firebase (đã làm trên Console)
- Seed `/devices/w3lYinUKv0NQo0w2PwhpbJ0U3c62 = true` (boolean) — tài khoản thiết bị đủ quyền ghi.

---

## 2. Cần làm thêm

### 2.1. Tự làm được (chỉ cần máy + source, KHÔNG cần ESP)
- [ ] **Nghiệm thu web online/offline**: chạy `tools/data-injector` (điền `config.js`) → mở web thấy
      "Trực tuyến"; tắt injector chờ ~15s → phải lật "Mất kết nối".
- [ ] **Test PWA**: chạy `tools/mock-esp-server` + serve `mobile/` → thử checkbox WiFi/MAC + nút `/info`.
- [x] **Deploy web dashboard** (hosting) — đã live bản mới (verify: `isDeviceOnline`/`lastSeen`).
- [x] **Deploy lại PWA** — đã live bản mới (verify: checkbox WiFi/MAC + nút /info).
- [ ] Xác nhận đã **deploy Rules** (`firebase deploy --only database`) — chưa soi được từ ngoài.
- [ ] **Commit + push GitHub** cho nhóm lấy code.
- [ ] Xoá node rác **`/gateway`** ở gốc RTDB; kiểm tra **`/admins`** đã có UID Google của admin chưa
      (thiếu thì web lưu `hset/deadband` bị `permission-denied`).

### 2.2. Cần con ESP thật (nhờ đồng đội có board)
- [ ] **Build-verify trước khi nạp**: `pio run -e esp1-main` — thay đổi stream→poll `/config` +
      partition `huge_app.csv` CHƯA được compile-test (máy dev không có toolchain PlatformIO).
- [ ] **Nạp firmware** (`pio run -e esp1-main -t upload`, tương tự esp2) — cần cáp USB + PlatformIO.
- [ ] Xem Serial xác nhận: login Firebase OK (pass `123456` đúng?) + dòng `[FB] Da day (2 request): ...`.
- [ ] Test thật: nút BOOT → SoftAP → app cấu hình; ESP-NOW 2 board; relay theo Deadband.
- [ ] **Push rules repo CÙNG LÚC nạp firmware repo** (vì rules bắt `esp1Online` bool; firmware cũ ghi
      `"on"` chuỗi sẽ bị từ chối).

---

## 2.3. Phát hiện mới: lỗi "mobile lỗi, laptop OK" khi test PWA — KHÔNG phải tràn RAM/flash
Nhóm báo #5 "APP CONFIG" test laptop OK nhưng mobile lỗi, nghi tràn bộ nhớ. **Đã xác định KHÔNG phải
vậy** (tràn RAM/flash không thể phân biệt laptop/mobile — đó là thuộc tính firmware, không đổi theo
client). Nguyên nhân thật (xem ảnh chụp màn hình team gửi): **Chrome Local Network Access (LNA)** —
cơ chế MỚI của Chrome/Android xin quyền khi trang HTTPS truy cập IP mạng cục bộ (192.168.4.1). Hộp
thoại xin quyền bị Android **chặn** vì có app khác đang **vẽ đè màn hình** ("Hiển thị đè lên ứng
dụng khác") → permission không bao giờ được cấp → mọi request tới ESP fail.
- **Fix cho nhóm:** tắt quyền "Hiển thị đè lên ứng dụng khác" của app đang chạy nền (Messenger,
  launcher...) trên điện thoại demo → mở lại PWA → prompt Allow/Block sẽ hiện được → bấm Allow.
  Xem chi tiết từng bước trong `mobile/README.md` mục "Local Network Access".
- **Đã cập nhật code:** `mobile/js/app.js` (`detectMixedContentRisk`, `handleFetchError`) — thông
  báo lỗi giờ phân biệt rõ LNA vs mixed-content, hướng dẫn đúng cách fix cho từng trường hợp.
- ⚠️ **Khuyến nghị demo:** dùng trang form do chính ESP phục vụ (`http://192.168.4.1/`, same-origin)
  làm phương án CHẮC ĂN — không phụ thuộc quyền LNA hay app nào đang chạy nền trên máy demo. PWA
  HTTPS chỉ nên là "nice to have" cho hôm demo, không phải đường duy nhất.
- ✅ **Nâng cấp thêm — CAPTIVE PORTAL** (`firmware/lib/common/provisioning.cpp`): thêm `DNSServer`
  điều hướng mọi tên miền về IP ESP khi ở chế độ SoftAP; `handleNotFound()` redirect (302) request GET
  không khớp về `/` → hệ điều hành (Android/iOS/Windows) tự phát hiện "mạng có captive portal" và
  **tự bật trình duyệt** ngay trang cấu hình sau khi nối AP — khỏi cần tự gõ IP hay mở PWA. Né hoàn
  toàn LNA/mixed-content vì same-origin HTTP ngay từ đầu. Đây giờ là đường khuyến nghị CHÍNH cho demo
  (PWA giữ lại chỉ để tiện quét QR lấy MAC). ⚠️ Chưa build-verify (không có toolchain ở máy này).

---

## 3. Vướng mắc / vấn đề mở

- 🟠 **Tràn flash/RAM trên ESP1** (checklist Embed mục 5 "APP CONFIG... Try hard", cũng là nguyên nhân
  stack overflow nhóm báo trước đó — gửi Firebase/TLS ngốn RAM+stack): **ĐÃ áp đủ 4 lớp mitigation
  vào firmware repo, CẦN KIỂM CHỨNG TRÊN BOARD (build + chạy thật) vì máy này không có toolchain
  PlatformIO để build-test:**
  1. `board_build.partitions = huge_app.csv` (`platformio.ini`) — bỏ vùng OTA thứ 2 không dùng, dồn
     ~3MB cho 1 vùng app (mặc định "esp32dev" chỉ ~1.25MB/vùng, dễ tràn với thư viện Firebase nặng).
  2. `SET_LOOP_TASK_STACK_SIZE(16*1024)` — stack loopTask 8→16KB.
  3. Gộp push 9→2 request (`setJSON`).
  4. **Đổi `/config` từ STREAM sang POLL ~10s** — bỏ hẳn `FirebaseData fbStream` riêng, dùng CHUNG
     `fbdo` cho cả đọc `/config` lẫn ghi `/sensor`,`/status` → chỉ còn **1 kết nối TLS** tại 1 thời
     điểm thay vì 2 (mỗi kết nối TLS tốn ~40-50KB RAM) — đây là khoản RAM lớn nhất cắt được. Đánh đổi:
     hset/deadband cập nhật trễ tối đa ~10s thay vì tức thời (chấp nhận được).
  - API `Firebase.RTDB.getJSON()` + `fbdo.to<FirebaseJson*>()` đã **xác nhận đúng** qua soi trực tiếp
    `addons/RTDBHelper.h` của thư viện mobizt trên GitHub (không đoán suông).
  - Log `freeHeap` mỗi vòng đẩy để chẩn đoán tiếp nếu vẫn crash: `freeHeap` tụt gần 0 → cân nhắc nâng
    stack lên 24KB hoặc giãn `PUSH_INTERVAL_MS`/`CONFIG_POLL_INTERVAL_MS` thêm.
  - ⚠️ **Bắt buộc chạy `pio run -e esp1-main` để build-verify trước khi nạp** — thay đổi API Firebase
    (stream→poll) chưa được compile-test do máy này thiếu toolchain.
- 🟠 **Password `123456` chưa xác minh** trên thiết bị thật — mới là phỏng đoán; nạp ESP mới biết đúng/sai.
      Sai thì reset password trong Firebase Console rồi cập nhật `HC_DEV_PASS`.
- 🟠 **Code phân kỳ**: nhóm đang chạy bản firmware/app **cũ đã deploy** (khác bản repo này). Khi áp bản
      mới phải hợp nhất, tránh chạy lẫn lộn (rules bool vs firmware chuỗi `"on"`).
- 🟡 **Giới hạn PWA (web)**: không đọc được SSID/IP điện thoại, không auto-connect WiFi (đó là khả năng
      của app native/SmartConfig). Đã thay bằng nút test `/info`. Nhóm đã đồng ý.
- 🟡 **Chưa deploy gì**: tất cả thay đổi ở source; chưa có tác dụng thật cho tới khi nạp firmware +
      `firebase deploy` + deploy PWA (3 đích khác nhau).

---

## 4. Quyết định đã chốt
- **Provisioning: giữ PWA tự viết** (SoftAP + REST "nối AP → gọi API"), **KHÔNG** dùng app Espressif
  chính chủ (app đó thiên về dev: cần đọc QR/POP qua terminal).

---

## 5. Thông số cấu hình cần nhớ
| Mục | Giá trị |
|---|---|
| Tài khoản thiết bị (email) | `device@smarthumidity.iot` |
| Mật khẩu thiết bị | `123456` (chờ xác minh) |
| Device UID (seed `/devices`) | `w3lYinUKv0NQo0w2PwhpbJ0U3c62` |
| WiFi hardcode fallback (dev) | SSID `chipchip` / pass `244466666` |
| Nút vào provisioning | GIỮ **BOOT (GPIO0)** lúc khởi động |
| Ngưỡng online (web) | `now − lastSeen < 15s` |
| Database URL | `https://smart-humidity-iot-default-rtdb.asia-southeast1.firebasedatabase.app/` |
