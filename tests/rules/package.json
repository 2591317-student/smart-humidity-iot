{
  "name": "smart-humidity-rules-tests",
  "private": true,
  "type": "module",
  "description": "Kiểm thử Security Rules của Realtime Database bằng Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only database --project demo-smart-humidity \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2"
  }
}
